{
  "name": "@dropseed/codeplus",
  "version": "0.3.0",
  "author": "Dave Gaeddert <dave.gaeddert@dropseed.dev>",
  "license": "MIT",
  "description": "Make your code elements interactive.",
  "files": [
    "dist",
    "index.js"
  ],
  "scripts": {
    "build": "rm -rf dist && parcel build browser.js",
    "watch": "parcel watch browser.js",
    "format": "prettier --write *.js",
    "pre-commit": "prettier --check *.js && npm run test",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/core": "^7.17.10",
    "@babel/preset-env": "^7.17.10",
    "babel-jest": "^28.0.3",
    "jest": "^28.0.3",
    "jest-environment-jsdom": "^28.0.2",
    "parcel": "^2.5.0",
    "prettier": "^2.6.2"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
